pending:
  - type: subagent_completion
    subagent_task: builder-agent-S42
    session_id: 1a7522d0-f4ad-4f51-85aa-56a7226581f8
    status: completed
    summary:
      filter: DemuxerFilter
      filter_name: builtin.player.demuxer
      stream_types:
        - DOLBY
        - ENCODED_AUDIO
        - RAW_AUDIO
        - ENCODED_VIDEO
        - SUBTITLE
      key_features:
        - 多轨并发读循环
        - PTS管理（AVI/MPEGPS/WMV）
        - 双模式切换
        - Seek三分支
        - DRM回调
      pipeline: SourcePlugin(S38) → DemuxerFilter(S41) → DecoderFilters
      git_commit: 4ef8a8f
      evidence_count: 20
      awaiting: approve/revise/reject/hold
  - type: approval_request
    mem_id: MEM-ARCH-AVCODEC-S39
    title: "AVCodecVideoDecoder 视频解码器核心实现——CodecBase/VideoDecoder/VideoDecoderAdapter 三层架构"
    scope: [AVCodec, VideoDecoder, CodecBase, VideoDecoderAdapter, Filter, HardwareCodec, SoftwareCodec]
    pipeline_position: "FilterPipeline 中游：DemuxerFilter(S41) → VideoDecoderFilter → AudioDecoderFilter(S35)"
    status: pending_approval
    created_by: builder-agent
    created_at: "2026-04-25T22:42:51+08:00"
    evidence_count: 20
    key_features:
      - 三层调用链（VideoDecoderAdapter→VideoDecoder基类→HDecoder/HEVCDecoder/VPXDecoder）
      - State 枚举十一状态（UNINITIALIZED→FROZEN）
      - Owner 枚举四态缓冲区所有权
      - BlockQueue 三队列（inputAvailQue/codecAvailQue/renderAvailQue）
      - Surface/Buffer 双模式切换
      - VideoDecoderFactory CreateByMime/ByName 双工厂
    awaiting: approve/revise/reject/hold
